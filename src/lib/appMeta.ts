// Single source of truth for the version string shown in the UI. Updated by the
// release script alongside package.json so the sidebar tag never drifts from
// the actual installer version.
export const APP_VERSION = '1.14.0';

